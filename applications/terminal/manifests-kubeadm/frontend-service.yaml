apiVersion: v1
kind: Service
metadata:
  name: terminal-frontend
  namespace: terminal
  labels:
    app: terminal-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: terminal-frontend

