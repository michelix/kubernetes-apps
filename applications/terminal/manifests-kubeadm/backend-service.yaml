apiVersion: v1
kind: Service
metadata:
  name: terminal-backend
  namespace: terminal
  labels:
    app: terminal-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: terminal-backend

